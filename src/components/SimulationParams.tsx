import "./SimulationParams.css";

export default function SimulationParams(): React.JSX.Element {
    return (
        <div className="container simulation-params">
            <h2>Параметры симуляции</h2>
            <hr/>
        </div>
    );
}